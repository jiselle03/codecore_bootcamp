<main class="container">
<p>&nbsp;</p>
<h3><%= @project.title %></h3>

<p><%= @project.description %></p>
<small>Due Date: <%= @project.due_date.strftime("%B %d, %Y")  %></small><br>
<small>Added <%= time_ago_in_words(@project.created_at) %> ago • Updated <%= time_ago_in_words(@project.updated_at) %> ago</small><br>
<br>

<%= link_to("Edit", edit_project_path(@project.id), :class => "btn btn-outline-primary") %>
<%= link_to("Delete", project_path(@project), method: :delete, data: {confirm: 'Are you sure?'}, :class => "btn btn-outline-primary") %>

</main>