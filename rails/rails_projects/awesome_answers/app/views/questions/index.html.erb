<h1>All Questions</h1>

<% @questions.each do |question| %>
<div>
    <h2><%= link_to(question.title, question_path(question.id)) %></h2>
    <p><%= question.body %></p>
    <p>Posted by <%= question.user.full_name %> <%= time_ago_in_words(question.created_at) %> ago</p>
</div>
<% end %>